<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP (cleartext) for your development server -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">"https://bhasamitra-backend.onrender.com/"</domain>
        <!-- 👆 Replace this with your laptop's IPv4 address -->
    </domain-config>

    <!-- Optional: Allow localhost for emulator -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">0.0.0.0:10000</domain>
    </domain-config>
</network-security-config>
